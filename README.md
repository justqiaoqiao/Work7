# Taichi 质点-弹簧布料物理模拟实验
苏文丽202411081022
## 项目简介
基于 Taichi GPU 并行框架实现质点-弹簧布料物理模拟，实现显式、半隐式、隐式欧拉三种数值积分求解器，搭配 GGUI 交互面板实时切换算法、重置场景，对比不同积分方案的模拟稳定性。

## 实验目标
1. 掌握 Taichi 3D 动态场景渲染，使用 GGUI 搭建交互控制面板
2. 理解质点-弹簧物理模型，实现弹力、阻尼力计算，通过速度钳制处理数值爆炸
3. 独立编写三种数值积分求解器，对比物理模拟稳定性差异
4. 掌握 `ti.kernel` / `ti.func` 编写 GPU 并行代码，理解状态同步与 Kernel 性能优化

## 实验原理
### 1. 质点-弹簧模型
布料离散为网格质点集合，质点间通过结构弹簧连接，基于胡克定律计算弹力，引入阻尼力防止系统能量发散。
弹簧弹力：
$$f_{a} = -k_{s} (|x_a - x_b| - l) \frac{x_a - x_b}{|x_a - x_b|}$$
阻尼力：
$$f_{d} = -k_{d} v_{a}$$

### 2. 数值积分方法
依据牛顿第二定律 $a=F/m$，时间步长 $\Delta t$ 内更新质点速度与位置：
- **显式欧拉**
$$
\begin{cases}
x_{t+1} = x_{t} + v_{t} \Delta t \\
v_{t+1} = v_{t} + a_{t} \Delta t
\end{cases}
$$
- **半隐式欧拉**
$$
\begin{cases}
v_{t+1} = v_{t} + a_{t} \Delta t \\
x_{t+1} = x_{t} + v_{t+1} \Delta t
\end{cases}
$$
- **隐式欧拉（定点迭代近似求解）**
$$
\begin{cases}
v_{t+1} = v_{t} + a_{t+1} \Delta t \\
x_{t+1} = x_{t} + v_{t+1} \Delta t
\end{cases}
$$

## 实验任务与步骤
### 任务1 场景初始化
- 自定义布料网格尺寸，初始化质点位置、速度、受力与弹簧拓扑结构
- 拆分多个 `@ti.kernel` 分步初始化，保证 GPU 多线程状态同步

### 任务2 力学计算与数值防爆
- `compute_forces_on()`：计算重力、阻尼力、弹簧力，使用 `ti.atomic_add` 规避多线程写入冲突
- `clamp_velocity()`：限制质点最大速度，抑制数值爆炸
- 力学函数封装为 `@ti.func`，编译内联减少 GPU 函数调用开销

### 任务3 积分求解器（核心）
实现三个独立计算 Kernel：
- `step_explicit()`：显式欧拉求解
- `step_semi_implicit()`：半隐式欧拉求解
- `step_implicit_iter()`：隐式欧拉定点迭代求解
优化方案：受力计算与状态更新合并至同一 Kernel，减少 Kernel 启动次数

### 任务4 渲染与 GGUI 交互
- 使用 `ti.ui.Window` 构建 3D 布料渲染场景
- GGUI 控制面板功能：切换积分算法、暂停模拟、一键重置布料

## 效果说明
调节阻尼系数对比三种积分算法表现：
### 阻尼系数 $k_d=1.0$
![kd=1](work7/work7_images/k_d=1.gif)
- 阻尼=1.0：低阻尼环境，显式欧拉易抖动、数值爆炸；半隐式稳定性中等；隐式欧拉稳定性最佳

### 阻尼系数 $k_d=5.0$

![kd=5](work7/work7_images/k_d=5.gif)
- 阻尼=5.0：高阻尼环境，布料震荡明显减弱，三类算法稳定性均提升

## 环境依赖
- Python 3.8+
- taichi >= 1.4.0

## 选做部分


### 1. 弹簧模型完善 (Spring Types)
为了模拟真实布料的抗剪切和抗弯曲特性，我在结构弹簧（Structural）的基础上增加了剪切（Shear）与弯曲（Bending）约束：

* **结构弹簧 (Structural)：** 连接相邻节点，负责维护布料的基本形状。
* **剪切弹簧 (Shear)：** 连接对角线节点，限制布料的剪切变形，赋予其更强的结构强度。
* **弯曲弹簧 (Bending)：** 跳格连接节点（如 i, i+2），模拟布料的褶皱刚度，使布料下垂时产生更自然的褶皱效果。



### 2. 空间碰撞处理 (Collision Handling)
在场景中引入了球体碰撞检测。通过计算每个质点与球心距离，实现了物理响应：

* **检测机制：** 在每一步积分循环中，判定质点是否进入球体半径范围。
* **响应逻辑：** 使用**投影法（Projection Method）**，当发生穿模时将质点强制重置到球体表面，并加入阻尼系数减少法向速度，防止布料在球体上过度反弹。
* **视觉优化：** 引入了微小的表面偏移量（Offset），有效解决了布料与球体碰撞时的渲染深度冲突（Z-fighting）。

### 3. 交互与控制
* **实时重置：** 为方便调试，添加了键盘 `R` 键监听功能，可随时触发 `init_cloth()` 重新初始化系统，实现一键重置模拟场景。

---
*实验记录：通过调整 `k_s`、`k_sh` 和 `k_b` 等系数，可以直观地观察到布料从“丝绸质感”到“纸张质感”的变化过程。*

安装命令：
```bash
pip install taichi
